<t-navbar title="设置" left-arrow />
<view class="setting">
  <t-cell-group wx:for="{{menuData}}" wx:for-item="item" wx:key="index" t-class="cell-group-class" theme="card">
    <t-cell
      wx:for="{{item}}"
      wx:for-item="carditem"
      wx:key="cardkey"
      title="{{carditem.title}}"
      hover
      arrow
      url="{{carditem.url}}"
      bordered="true"
      data-data="{{carditem}}"
      bindclick="onEleClick"
    >
      <image slot="left-icon" style="width:40rpx;height:40rpx" mode="aspectFit" src="{{ menuIcon }}" />
    </t-cell>
  </t-cell-group>
</view>

<t-toast id="t-toast" />
